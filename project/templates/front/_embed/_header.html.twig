<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('front_home') }}">Symfony App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
                aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                    <a href="{{ path('front_page', {'slug': 'about'}) }}"
                       class="nav-link{{ (app.request.get('slug') == 'about') ? ' active' : '' }}">About
                    </a>
                </li>
            </ul>
            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" role="button" aria-expanded="false">
                            <i class="fas fa-user fa-fw"></i> {{ app.user.userIdentifier }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a href="{{ path('front_profile_show') }}" class="dropdown-item"><i class="far fa-address-card fa-fw"></i> Profile</a></li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li>
                                    <a href="{{ path('admin_dashboard') }}" class="dropdown-item">
                                        <i class="fas fa-tachometer-alt fa-fw"></i> Admin Panel
                                    </a>
                                </li>
                            {% endif %}
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li><a href="{{ path('front_logout') }}" class="dropdown-item"><i class="fas fa-sign-out-alt fa-fw"></i> Logout</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('front_login') }}"
                           class="nav-link{{ (app.request.get('_route') == 'front_login') ? ' active' : '' }}">Login
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('front_register') }}"
                           class="nav-link{{ (app.request.get('_route') == 'front_register') ? ' active' : '' }}">Sign up
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
